# The build needs devDependencies (tailwindcss, @tailwindcss/vite, @nuxt/icon,
# @nuxt/eslint). Force them even when the platform installs in production mode
# (e.g. Railway/Nixpacks set NODE_ENV=production).
include=dev
